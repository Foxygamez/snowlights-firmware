name: Compile and Release Firmware

on:
  push:
    branches: [ main ]
    paths: [ '**.ino' ]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Prepare sketch folder
        run: |
          # Arduino requires sketch file to match folder name
          SKETCH=$(find . -maxdepth 1 -name "*.ino" | head -1)
          SKETCH_NAME=$(basename "$SKETCH" .ino)
          mkdir -p ./sketch/$SKETCH_NAME
          cp "$SKETCH" ./sketch/$SKETCH_NAME/$SKETCH_NAME.ino
          echo "Prepared: ./sketch/$SKETCH_NAME/$SKETCH_NAME.ino"

      - name: Compile with arduino-cli action
        uses: arduino/compile-sketches@v1
        with:
          fqbn: esp32:esp32:esp32
          platforms: |
            - name: esp32:esp32
              source-url: https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json
          libraries: |
            - name: FastLED
            - name: MPU6050_light
          sketch-paths: |
            - ./sketch
          cli-compile-flags: |
            - --output-dir=./build

      - name: Get firmware version
        id: version
        run: |
          VERSION=$(grep -o 'FW_VERSION "[^"]*"' *.ino | grep -o '"[^"]*"' | tr -d '"')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Firmware version: $VERSION"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.version }}
          name: Firmware v${{ steps.version.outputs.version }}
          body: Auto-compiled SnowLights firmware
          files: ./build/*.bin
          make_latest: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
